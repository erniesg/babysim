# Prototype

The first prototype should be a local static playable loop.

Required first modules:

- `LocalGameTransport`
- Director Runtime
- BabyAgent
- PartnerAgent
- AudioDirector
- Render loop
- Panic behavior
- Event log
- Fairness ledger

Only after the local loop works should Worker + Durable Object transport be added.

